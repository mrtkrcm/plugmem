[project]
name = "claude-code-plugmem"
version = "0.1.0"
description = "MCP server providing PlugMem tools to Claude Code"
requires-python = ">=3.10"
dependencies = []

[tool.mypy]
strict = true
python_version = "3.11"
exclude = ["tests/"]

[tool.ruff]
line-length = 120
target-version = "py311"
